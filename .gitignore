# Data (tracked curated inputs; ignore only local re-extractions)
data/raw/moseq_syllables_per_frame.csv
data/raw/manuscript_final/
data/**/*.pkl
data/**/*.parquet
data/**/*.npy

# Manuscript figures and curated report workbooks are tracked canonical outputs.

# Data files
*.pkl
*.parquet
*.npy

# The archived legacy classifier is the one deliberate pickle exception: these
# two files are the model of record behind Supplementary Figure 4 and the class
# order its import utilities read (docs/figure_structure.md).
!classifier/legacy_shap/xgb_model.pkl
!classifier/legacy_shap/label_encoder.pkl

# Python
.venv/
.uv-cache/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Excel lock files, written while a workbook is open
~$*.xlsx

# Local overrides
config/paths.py

# Local scratch logs / rebuild experiments
*.log
*_test.xlsx
*_rebuilt.xlsx
classifier/outputs/
classifier/*_smoke.joblib
data/processed/**/*_smoke.csv
