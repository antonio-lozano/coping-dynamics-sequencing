# Report Workbooks

`raw_data.xlsx` is the manuscript-facing raw-data workbook.

`statistical_report.xlsx` is the manuscript statistical report.

Both workbooks are generated from tracked repository files; no template workbook
is required.

```powershell
python scripts\build_raw_data_workbook.py
python scripts\build_statistical_report.py
```
