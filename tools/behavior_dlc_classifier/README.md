# Behavior Studio has moved

The application is maintained in [apps/behavior-dlc-classifier](../../apps/behavior-dlc-classifier/).
Use the installation and launch commands there. This directory is a navigation
notice, not an executable alias; cloning requires no symbolic-link privileges.
