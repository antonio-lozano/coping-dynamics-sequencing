# Results Outputs

`figure_data/` contains reproducible supporting tables and intermediate renders
written by the figure scripts.

`statistical_reports/` contains statistical CSV outputs and the final no-seed
Results text audit. Historical seeded Figure 4 files are recoverable from tag
`pre-noseed-seeded` or branch `backup-seeded-analysis`; they are not kept on the
current no-seed branch.
