# Results Outputs

`figure_data/` contains reproducible supporting tables and intermediate renders
written by the data-derived figure scripts.

`statistical_reports/` contains statistical CSV outputs and the final Results
text audit used for manuscript checks.
