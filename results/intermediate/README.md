# Intermediate Outputs

Helper outputs generated by the figure scripts.

- `figures/`: non-canonical renders used for audit. Canonical manuscript figures
  are in `figures/` at the repository root.
- `tables/`: helper tables, thresholds, distance scores, and audit summaries.

These files are tracked so reviewers can inspect intermediate analysis values
without rerunning the full plotting workflow.
